name: Architecture Lint

on:
  pull_request:
  push:
    branches:
      - main

permissions: read-all

jobs:
  architecture-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run architecture lint
        run: scripts/lint-architecture
